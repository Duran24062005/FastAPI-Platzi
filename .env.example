# App runtime
HOST=0.0.0.0
PORT=8000

# Security
JWT_SECRET_KEY=change_this_secret_in_production

# PostgreSQL connection
# Leave these unset to use the local SQLite fallback (`database.sqlite`)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=fastapi_platzi
